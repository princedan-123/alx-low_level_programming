#include <stdio.h>
int main(void)
{	/**
	 * main - prints "programming is like buikding a multilingual
	 *  puzzle, followed by a new line.
	 * Returns:Always 0.
	 */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
