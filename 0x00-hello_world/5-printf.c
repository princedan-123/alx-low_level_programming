#include <stdio.h>
/**
 * main - it prints with proper grammer but the outcome is a piece of art
 * Return: Always return 0.
 */
int main(void)
{	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
