#include <stdio.h>
/**
 * main - prints three alphabets.
 * Return: Returns 0.
 */
int main(void)
{	char ch = 'a'

	char Ch = 'A'
	while (ch <= 'z')
	{	putchar(ch); 
		ch++;
	}
	while (Ch <= 'Z')
	{	putchar(Ch); 
		Ch++;
	}
	Return (0);	
}
