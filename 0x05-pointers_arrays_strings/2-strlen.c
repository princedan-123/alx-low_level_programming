#include "main.h"
/**
 * _strlen - Returns the lenght of a string
 * @str: the string
 * Return: the lenght of the string
 */
int _strlen(char *s)
{
	size_t length = 0;

	while (*str++)
		length++;
	return (length);
}
