#include "main.h"
/**
 * wildcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: 1 or 0
 */
int wildcmp(char *s1, char *s2,)
{
	if (i != '\0')
	{
		if (*s1 == *s2)
			return (1);
	}
	if (i == '\0')
		return (0);
	return (s1++, s2++, wildcmp(s1, s2,);
}
