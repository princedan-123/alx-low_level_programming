#include "main.h"
#include <stdio.h>
/**
 * main - prints the name of the program
 * @argv: the parameter of the function
 * @argc: parameter of the function
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void) argc;
	return (0);
}
